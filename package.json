{
  "name": "warehouse-builder",
  "private": true,
  "version": "1.0.0",
  "description": "Dynamic Spatial Model Builder — Spatialis OS digital twin PWA with a Vercel Postgres backend",
  "type": "module",
  "scripts": {
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0",
    "nodemailer": "^9.0.5"
  }
}
